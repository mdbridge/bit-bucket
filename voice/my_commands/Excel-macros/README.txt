To create permanent Excel macros, use

    View->Macros->Record macro...

put in personal workbook, use shortcut desired, description if wanted


======

   * Excel 2007 installation of move graph procedure:
     * create personal workshop spreadsheet
       * only way know at moment is to record a macro & select "personal
         macro workbook" as destination (Store macro in:); then delete
         module containing macro
     * developer->Visual Basic
       * click on personal worksheet then file->import: 
         ~/voice/my_commands/Excel-macros/module1.bas
     * Excel window->developer->macros
       click on macro, options, set shortcut to ^q
